[Unit]
Description=UFOBeep all external feeds ingest
After=network-online.target

[Service]
Type=oneshot
ExecStart=/usr/bin/curl -fsS -X POST -H "Authorization: Bearer ${UFOBEEP_ADMIN_TOKEN}" https://api.ufobeep.com/admin/feeds/all/run
EnvironmentFile=/etc/ufobeep.env
